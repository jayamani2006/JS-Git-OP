# GitForge Studio V1 uses only the Python standard library
# (tkinter, subprocess, pathlib, dataclasses, json) so there is nothing
# to pip-install. This file exists so tooling/CI that expects one doesn't break.
#
# Requirements:
#   Python 3.8+  (tkinter must be included in your Python install — it is
#                 on python.org Windows installers by default)
#   Git for Windows (git.exe on PATH)  https://git-scm.com/downloads
